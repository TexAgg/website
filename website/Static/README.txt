Folder for static html projects,
now hosted on the website since Dropbox is ending public hosting.

These are all git submodules.
If any of the repositories are private, 
then the modules must be updated via the command line,
instead of through MonoDevelop (because a password is needed).